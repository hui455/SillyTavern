# 如何为 SillyTavern 做贡献

## 搭建开发环境

1. 必备软件：git 和 node。
2. 推荐编辑器：Visual Studio Code。
3. 你也可以使用 GitHub Codespaces，它会帮你把所有环境都配置好。

## 获取代码

1. 注册一个 GitHub 账号。
2. 把本仓库 fork 到你的账号下。
3. 将 fork 克隆到你的机器上。
4. 在代码编辑器中打开克隆下来的仓库。
5. 创建一个 git 分支（推荐），如果还没读过 [git 书籍](https://git-scm.com/book/en/v2/Getting-Started-About-Version-Control)，可以先看看。
6. 做出你的改动并在本地测试。
7. 提交改动并把分支推送到远程仓库。
8. 到 GitHub 上发起一个 pull request，目标指向合适的上游分支。

## 贡献指南

### 保持代码质量

我们的标准并不算高，但请确保代码不要太难看：

- 完成后运行 VS Code 的自动格式化。
- 运行 `npm run lint` 用 ESLint 检查，然后修复错误。
- 运用常识，遵循现有的命名约定。

### 使用正确的目标分支

请为 `staging` 分支创建 pull request，99% 的贡献都应该提交到那里。这样别人可以在下一个稳定版发布之前先测试你的代码。

以下场景你仍然可以给 `release` 分支提交 pull request：

- 更新 README。
- 更新 GitHub Actions。
- 修复关键 bug。

项目维护者会在合并之前测试你的代码，也可能会修改你的代码。为了让工作流程顺畅，请确保以下事项：

- 勾选 "Allow edits from maintainers" 选项。
- 一旦 PR 脱离草稿状态，避免对分支进行 force-push。

### 让贡献保持小而可测试

为了确保你的贡献可以被测试和审查，请尽量不要让每个 pull request 超过 **200 行代码**（增删都算）的软上限。如果改动更多，请拆成多个 pull request。

对于更大的改动，我们也可以考虑创建单独的特性分支，但请先与维护者讨论。例如：

- 把较大的主 PR 保持为草稿，用它来讨论实现方案。
- 把每组函数或功能拆成约 200 行的 PR，以便被正确审查并合并到 staging 或特性分支。
- 如果存在无法拆分的大规模相互依赖改动，先从使用最多的依赖开始，并为依赖函数写桩代码。
- 每个 PR 会逐一被审查和测试，就绪后合并进特性分支。
- 不要提前创建所有分支，因为之前提交中因测试/审查而产生的后续改动可能会造成大量合并冲突。

### 提供清晰的改动说明

请写出至少有一定意义的 PR 描述和提交信息。没有"标准"写法，但以下内容可能有助于勾勒整体结构：

- 改动的原因是什么？
- 你做了什么来实现它？
- 审查者应如何测试这个改动？

### 我们（很可能）不会你的语言

英语是该项目的主要交流语言。在写提交信息、PR 描述、评论和其他文本时，请只使用英语。这不适用于本地化文件的贡献。

### 法律相关

请注意许可证。你的贡献将以 GNU Affero General Public License 授权。如果你不清楚这意味着什么，请咨询你的律师。

## AI 编码辅助工具的使用（"Vibe Coding"）

我们不禁止也不鼓励使用 AI 工具辅助编码来帮助你编写代码、文档等。这包括专门的 IDE、插件和扩展、聊天界面等。但请记住以下几点：

- 无论代码是谁（或什么）写的，你都要对它负责。提交前请仔细审查和测试一切内容，并准备好讨论和修复审查中出现的任何问题。
- 维护者可以拒绝审查和接受质量极低的 PR，即修复问题的时间超过从头重写代码的时间。
- 避免 AI 工具的常见错误，例如：添加/删除无关的注释、过度的日志输出、不了解项目上下文和约定等。
- 你可以（但并非必须）触发维护者添加到项目中的 AI 工具（Gemini、Copilot、Codex）。请记住，这些工具生成的任何反馈（评论、建议）都不是行动指令；在采用之前请妥善评估。

## 延伸阅读

1. [如何编写 UI 扩展](https://docs.sillytavern.app/for-contributors/writing-extensions/)
2. [如何编写服务端插件](https://docs.sillytavern.app/for-contributors/server-plugins)